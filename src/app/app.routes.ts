import { Routes } from '@angular/router';
import { VehicleInfoComponent } from './components/vehicleInfo/vehicleInfo.component';

export const routes: Routes = [{ path: 'vehicles', component: VehicleInfoComponent },];
